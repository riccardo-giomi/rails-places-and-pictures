- # locals: (pictures:)

div class="font-bold mt-4"
  = "#{place_pictures_count(pictures).capitalize}:"
- unless pictures.empty?
  div class="w-full"
    - pictures.each do |picture|
      div id="#{dom_id picture}" class="content-box my-2 shadow-sm"
        div class="flex gap-x-2 items-center"
          div class="w-fit min-w-[300px] text-center"
            - if picture.file.attached?
              = render 'application/image', image: picture.file
            - else
              span class="italic" No image yet
          div class="flex-grow"
            div class="text-xl font-bold"
              = picture.description
            - if picture.notes.present?
              div class="my-3 italic"
                = picture.notes

- else
  span class="italic font-medium" There are no images
